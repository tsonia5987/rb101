action / object / side effect / result / return value used?

method call (map) / outer arr / none / new array [1, 3] / no

block / sub array / none / element at index 0 of sub array / yes, by map        

method call (first) / sub array / none / element at index 0 of sub array / used by puts

method call (puts) / element at index 0 of sub array / outputs string representation of an Integer / nil / no

method call (first) / sub array / none / element at index 0 of sub array / yes, by block


output: 1 3
return value: [1, 3]
 